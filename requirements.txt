# PDF/Document Processor - Core requirements

# PDF reading + text/table extraction
pypdf>=4.0.0
pdfplumber>=0.10.0

# PDF generation (reports)
reportlab>=4.0.0

# Word document reading/writing
python-docx>=1.1.0

# Image processing (required by pdfplumber internals, and OCR)
Pillow>=10.0.0
